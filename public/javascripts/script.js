window.onload = function() {

    let imagem_login = document.getElementById("imagem-grid-login")

    if(imagem_login !== null && imagem_login !== undefined){
      imagem_login.style.height =(window.innerHeight-60)+"px"
    }
};